```text
あなたはプロダクト紹介記事を得意とする日本語コピーライター兼マーケターです。以下のURLに掲載されている「アプリ紹介文（VidPair）」を一次情報として読み取り、内容を踏まえたうえで、“人間が書いた記事っぽい”温度感と流れで、ダウンロードしたくなる紹介記事をHTMLとして新規に書いてください。

参照URL：
https://izanami.dev/post/33e9d978-ff48-418e-9b9e-37fe23e66309

【タスク】
1) URL本文を解析し、次を正確に把握する（※この分析結果は出力しない）
- アプリ名、対象ユーザー、解決する課題、主要機能、利用シーン、導入効果、料金/課金形態、対応端末/言語、プライバシー上の扱い、レビュー要旨、ダウンロード導線

2) 1を踏まえ、元文の良い点は残しつつ「用途・利点・使いどころ・導入後の変化」がより伝わるように、文章を最初から書き直して出力する
- 事実（料金/対応OS/機能など）の数値や仕様は、URLの内容と矛盾させない
- 元文にない情報を断定で捏造しない（不明点は一般的な表現で価値訴求し、断定しない）
- 「AI」「生成」「プロンプト」など、制作側の都合が透ける言葉は本文に出さない

【章立て（必ず5章）】
- 全体タイトル：<h1>（アプリ名＋一言ベネフィットを含める）
- 以降、<h2>で5章を構成し、各章の本文は「日本語500文字以上」（5章すべて）
- 章テーマはダウンロード促進に最適になるようにあなたが設計する（例：概要/悩みと解決/ユースケース/機能とメリット/はじめ方とCTA 等。章数は5で固定）
- ただし“毎章同じ型”にしない（章ごとに語り口・展開の仕方・テンポを変える）

【トーン（砕けた口調・購買意欲を上げる）】
- 文体は、友達におすすめするような砕けた口調。敬語は最小限（です/ますでもOKだが硬くしない）
- 読者を「あなた」と呼びかけ、使用シーンが浮かぶように書く（例：「練習終わりにこれで見返すと…」）
- 機能説明より「体験の変化（Before/After）」を優先する（ただし誇張しすぎない）
  - Before：迷う/時間が溶ける/直しどころが分からない
  - After：気づける/直す場所が決まる/練習の質が上がる感じがする
- 感情が動く言い回しはOK（「地味に助かる」「これ便利」「ここ刺さる」等）だが、連発しない
- 強すぎる断定・煽りは避ける（禁止ワード例：「画期的」「革命的」「必ず」「絶対」「100%」など）

【“AIっぽさ”を減らすために避けるべき言い回し・構成（重要）】
- 同じ接続語・定型句の連打（「さらに」「また」「一方で」「結論として」などを繰り返さない）
- 章ごとに同一のテンプレ構文（毎回「結論→理由→まとめ」を固定しない）
- 似た意味の言い換えで水増ししない（冗長な同義反復を避け、具体例・場面で厚みを出す）
- 文の長さ・リズムを均一化しない（短文/中文/長文を混ぜる）
- 箇条書きだらけにしない（必要なら使ってもよいが、文章の流れを主役に）
- 「刺さる一文：」のようなラベル付けはしない（強調したい短文は“自然に”単独行として紛れ込ませる）
- 無難で客観的すぎる説明文に寄せない（人が書く記事の“間”や“視点”を入れる）

【人間が書いた記事っぽくする言い回し・構成（積極採用）】
- “具体の一場面”から入る（例：「練習動画撮ったのに、見返すと何が違うか分からない…」みたいな状況描写）
- 章の途中で小さな寄り道（短い補足・つぶやき・意外な気づき）を入れて、一本調子を避ける
- 語彙と表現を散らす（同じ意味でも語や比喩、例え、場面を変えて展開する）
- 断定ではなく「〜になりがち」「〜しやすい」「〜な人には刺さる」などの“現実的な言い方”で背中を押す
- 読者が行動しやすい具体提案を入れる（「今日はここだけ見る」など）

【利用シーン（ユースケース）要件】
- 利用シーンを扱う章では「定番 → 意外 → さらに意外」の順で書く
- 意外な用途は最低2つ入れる（URL内容と矛盾しない範囲で）
- 「誰が」「どんな場面で」「どう使い」「何が良くなるか」がイメージできる具体例にする

【表現・構造の必須要件】
- 5章のうち少なくとも1回は、視覚的に理解しやすい表現を入れる（必須）
  - 例：HTMLの <table> で「プラン差」「機能一覧」「利用シーン×効果」などを整理
  - 表は <table><thead><tbody> を用い、見出し列/行が分かるようにする

【最後の章（背中押し要件）】
- 必ず含める：
  - 「まずはこれだけやってみて」の1分スタート手順（3ステップ）
  - 「合わなかったらこう使えばOK」の逃げ道（ライトな使い方提案）
  - CTAは圧強めにしないが、行動が具体的になる言い方にする（例：「今日の練習動画で1回だけ試してみて」）
- CTAでは、URLに書かれている導線（ストア等）に矛盾しない表現にする

【出力構成（ChatGPTが読み取りやすい形）】
- 各章は必ず次の2要素を明示的に分けて出力する（見出しと本文の区別を明確にする）
  - 見出し：<h2>...</h2>
  - 本文：<div class="section-body">...</div>
- <br> による改行挿入は「本文（section-body）の中だけ」に適用する（見出しや表の中には不要）
- 章と章の間には区切りとして必ず <hr> を入れる

【出力ルール】
- 出力は以下jsonのみ（前置き、方針説明、注意喚起、解析結果の開示、Markdown、コードフェンスは一切出さない）
- 以下jsonに当てはめるように出力して。
- URLはMarkdown形式（[...](...)）を禁止し、必ず生URLのみ（https://...）を入れてください。
例：
- OK: "url": "https://example.com"
- NG: "url": "[https://example.com](https://example.com)"
- bodyHtml にHTML文字列を入れる場合、JSONとして壊れないように必ずエスケープしてください。

- bodyHtml は JSON文字列として出力すること
- HTML内の " は必ず \" にすること
- 改行は \n にすること
- 文字列の区切りは必ず "（ダブルクォート）で統一


{
  "article": {
    "bodyHtml": "<h1>{{h1}}</h1>\n<div class=\"lead\">{{leadHtml}}</div>\n\n<h2>{{chapter1Title}}</h2>\n<div class=\"section-body\">{{chapter1BodyHtml}}</div>\n<hr>\n\n<h2>{{chapter2Title}}</h2>\n<div class=\"section-body\">{{chapter2BodyHtml}}</div>\n<hr>\n\n<h2>{{chapter3Title}}</h2>\n<div class=\"section-body\">{{chapter3BodyHtml}}</div>\n<hr>\n\n<h2>{{chapter4Title}}</h2>\n<div class=\"section-body\">{{chapter4BodyHtml}}</div>\n<hr>\n\n<h2>{{chapter5Title}}</h2>\n<div class=\"section-body\">{{chapter5BodyHtml}}</div>",
    "emphasisLine": "{{emphasisLine}}",
    "followMainText": "{{followMainText}}"
  },

  "video": {
    "enabled": "true",
    "ariaLabel": "{{videoAriaLabel}}"
  },

  "linksBlock": {
    "enabled": "true",
    "headingText": "{{linksHeadingText}}",
    "links": [
      {
        "label": "{{link1Label}}",
        "url": "{{link1Url}}"
      }
    ]
  },

  "pager": {
    "enabled": "true",
    "prevText": "{{pagerPrevText}}",
    "homeText": "{{pagerHomeText}}",
    "nextText": "{{pagerNextText}}",
    "prevUrl": "{{pagerPrevUrl}}",
    "homeUrl": "{{pagerHomeUrl}}",
    "nextUrl": "{{pagerNextUrl}}"
  },

  "bigBanner": {
    "enabled": "true",
    "tiles": [
      {
        "imgAlt": "{{tile1Alt}}",
        "imgSrc": "{{tile1ImgSrc}}",
        "caption": "{{tile1Caption}}",
        "priceText": "{{tile1PriceText}}",
        "linkUrl": "{{tile1LinkUrl}}"
      }
    ]
  },

  "yellowPick": {
    "enabled": "true",
    "html": "{{yellowPickHtml}}"
  }
}
